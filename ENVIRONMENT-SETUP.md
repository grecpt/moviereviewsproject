### Install Django on the path

`` $ python -m pipenv install django

### Activate the virtual environment

`` python -m pipenv shell

### Create a new Project

`` python -m django startproject moviesreviews
